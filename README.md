# Gerador de Currículo (Flask)

## Rodar local
python -m venv .venv
# Windows:
.venv\Scripts\activate
# Linux/mac:
# source .venv/bin/activate

pip install -r requirements.txt
python app.py

Acesse: http://127.0.0.1:5000